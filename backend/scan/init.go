package scan

import (
	"github.com/pocketbase/pocketbase"
)

// InitHandlers initializes the scan handlers
func InitHandlers(app *pocketbase.PocketBase, ansibleBasePath string) {
	// Initialize any required handlers here
	// For now, this is a placeholder for future handler initialization
}
